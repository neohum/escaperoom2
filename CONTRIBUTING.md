# 기여 가이드

방탕출 교육 플랫폼 프로젝트에 기여해 주셔서 감사합니다! 🎉

## 🌿 브랜치 전략

- `main`: 프로덕션 브랜치 (안정 버전)
- `develop`: 개발 브랜치 (다음 릴리스 준비)
- `feature/*`: 새로운 기능 개발
- `fix/*`: 버그 수정
- `docs/*`: 문서 업데이트

## 📝 커밋 메시지 규칙

```
<type>: <subject>

<body>
```

### Type
- `feat`: 새로운 기능
- `fix`: 버그 수정
- `docs`: 문서 변경
- `style`: 코드 포맷팅 (기능 변경 없음)
- `refactor`: 코드 리팩토링
- `test`: 테스트 추가/수정
- `chore`: 빌드 프로세스, 도구 설정 등

### 예시
```
feat: 주관식 유사도 계산 기능 추가

- Levenshtein Distance 알고리즘 구현
- 60% 임계값 설정
- 테스트 케이스 추가
```

## 🔄 Pull Request 프로세스

1. **Fork & Clone**
   ```bash
   git clone https://github.com/neohum/escaperoom2.git
   cd escaperoom2
   ```

2. **브랜치 생성**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **개발 및 커밋**
   ```bash
   git add .
   git commit -m "feat: your feature description"
   ```

4. **Push**
   ```bash
   git push origin feature/your-feature-name
   ```

5. **Pull Request 생성**
   - GitHub에서 PR 생성
   - 템플릿에 따라 설명 작성
   - 리뷰어 지정

6. **코드 리뷰**
   - 리뷰어의 피드백 반영
   - 필요시 추가 커밋

7. **Merge**
   - Squash and Merge 사용
   - 커밋 메시지 정리

## 🧪 테스트

PR 전에 반드시 테스트를 실행하세요:

```bash
# Backend 테스트
cd backend
npm test

# Frontend 테스트
cd frontend
npm test
```

## 📋 코드 스타일

- **TypeScript**: ESLint + Prettier 사용
- **들여쓰기**: 2 spaces
- **따옴표**: Single quotes
- **세미콜론**: 사용

```bash
# 린트 검사
npm run lint

# 자동 수정
npm run lint:fix
```

## 🤝 행동 강령

- 존중과 배려
- 건설적인 피드백
- 협력적인 태도

## 📞 문의

질문이나 제안사항이 있으시면 Issue를 생성해 주세요!

---

**감사합니다!** 🙏

